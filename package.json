{
  "name": "orbit-console-nextjs-tests",
  "version": "1.0.0",
  "private": true,
  "description": "E2E browser + API tests for orbit_console_nextjs",
  "scripts": {
    "test:e2e": "playwright test",
    "test:api": "pytest tests/ -v -m console_api",
    "test:all": "playwright test && pytest tests/ -v -m console_api"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
